{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/api-client-react build && pnpm --filter unitech build",
  "outputDirectory": "unitech/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
